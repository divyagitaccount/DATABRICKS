# This is a Databricks asset bundle definition for dab_test1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_test1
  uuid: 4b1c1329-5240-491d-8fcd-ae5780308e12

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-656919d7-cd50.cloud.databricks.com/
    variables:
      catalog: gcp2
      schema: logisitics
  prod:
    mode: production
    workspace:
      host: https://dbc-656919d7-cd50.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/divyabharathikbdb88@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/divyabharathikbdb88@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: gcp2
      schema: logistics
    permissions:
      - user_name: divyabharathikbdb88@gmail.com
        level: CAN_MANAGE
